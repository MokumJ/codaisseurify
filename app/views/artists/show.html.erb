<div class="panel panel-primary">
  <div class="panel-heading"><%= @artist.name %></div>

  <div class="panel panel-default">
  <div class="panel-heading">
<%=image_tag @artist.picture.thumbnail %>
</div>
</div>
<h3> Songs<h3>
  <ol>
<%@songs.each do|song|%>
<p> <%=song.name%> </p>
  <%end%>
</ol>
  <div class="col-md-3 right">
    <%= link_to "Update", edit_artist_path(artist), class: "btn btn-default" %>
    <%= link_to "[Delete]", artist_path(@martist.id), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default" %>
  </div>
